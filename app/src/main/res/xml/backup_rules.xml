<?xml version="1.0" encoding="utf-8"?>
<!--
   Legacy Auto Backup rules (API <= 30). The app's minSdk is 34, so this file is
   effectively unused at runtime; data_extraction_rules.xml is authoritative. Kept in
   sync for correctness: exclude the privacy-sensitive prefs and usage DataStore.
-->
<full-backup-content>
    <exclude domain="sharedpref" path="swyplauncher_prefs.xml" />
    <exclude domain="file" path="datastore/" />
</full-backup-content>
